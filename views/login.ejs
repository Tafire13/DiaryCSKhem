<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/style.css">
</head>

<body class="bg-black font-sans antialiased">

    <div class="flex justify-center items-center w-full h-screen">

        <div
            class="flex flex-col items-center w-full max-w-md p-8 border border-gray-700 bg-gray-900/50 text-white rounded-2xl shadow-2xl backdrop-blur-sm">

            <h1 class="text-3xl font-bold tracking-wider mb-2">Login</h1>

            <form action="/login" method="POST" class="flex flex-col w-full mt-6 gap-4">

                <div class="flex flex-col gap-2">
                    <label class="text-gray-400 text-sm ml-1">Input Username</label>

                    <input type="text" name="username" placeholder="Username"
                        class="w-full px-5 py-3 rounded-xl bg-gray-800 border border-gray-600 text-white placeholder-gray-500 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/50 transition-all duration-200"
                        autofocus>
                </div>

                <button type="submit"
                    class="mt-2 w-full py-3 rounded-xl bg-blue-600 hover:bg-blue-500 text-white font-bold shadow-lg shadow-blue-500/30 transform hover:scale-[1.02] transition-all duration-200">
                    Login
                </button>

            </form>

        </div>
    </div>

</body>

</html>