<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Diary</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-black text-white font-sans antialiased">

    <div class="flex justify-center items-center min-h-screen p-4">

        <div class="w-full max-w-lg bg-gray-900 border border-gray-800 rounded-2xl shadow-2xl p-8">
            <h1 class="text-3xl font-bold mb-6 text-center">Write New Diary</h1>

            <form action="/upload" method="POST" enctype="multipart/form-data" class="flex flex-col gap-4">

                <div>
                    <label class="block text-gray-400 mb-2 text-sm">หัวข้อเรื่อง</label>
                    <input type="text" name="title" required
                        class="w-full px-4 py-3 rounded-xl bg-gray-800 border border-gray-700 focus:border-blue-500 focus:outline-none transition-colors">
                </div>

                <div>
                    <label class="block text-gray-400 mb-2 text-sm">รูปภาพความทรงจำ</label>
                    <div class="relative">
                        <input type="file" name="image" accept="image/*" required
                            class="w-full text-sm text-gray-400 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-blue-600 file:text-white hover:file:bg-blue-500 cursor-pointer bg-gray-800 rounded-xl border border-gray-700">
                    </div>
                </div>

                <div>
                    <label class="block text-gray-400 mb-2 text-sm">บันทึกเรื่องราว</label>
                    <textarea name="description" rows="4" required
                        class="w-full px-4 py-3 rounded-xl bg-gray-800 border border-gray-700 focus:border-blue-500 focus:outline-none transition-colors"></textarea>
                </div>

                <button type="submit"
                    class="mt-4 w-full py-3 rounded-xl bg-gradient-to-r from-blue-600 to-purple-600 text-white font-bold shadow-lg hover:scale-[1.02] transition-transform">
                    บันทึกไดอารี่
                </button>

                <a href="/" class="text-center text-gray-500 text-sm hover:text-white mt-2">ยกเลิก</a>
            </form>
        </div>
    </div>
</body>

</html>